<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanxi.mapper.LoginMapper">

    <!--根据用户名查询用户信息-->
    <select id="findUserByUsername" parameterType="string" resultType="com.wanxi.pojo.User">
        select * from t_user where username = #{username}
    </select>

    <!--根据用户id去查询到其所对应的权限信息-->
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.`perms`
        FROM
            t_user_role ur
                LEFT JOIN `t_role` r ON ur.`role_id` = r.`id`
                LEFT JOIN `t_role_menu` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `t_menu` m ON m.`id` = rm.`menu_id`
        WHERE
            user_id = #{userId}
          AND r.`status` = 0
          AND m.`status` = 0
    </select>


</mapper>